-- textual answer given by lua output
print("Ensure that when a variable goes out of scope in the implemented language (I call my language 'pug') then assign nil to the corresponding (shadow) variable in lua so that is is garbage collected by the lua garbage collector")
	
